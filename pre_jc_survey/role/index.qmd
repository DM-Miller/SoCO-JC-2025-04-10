---
title: "Role"
include-in-header: 
  - text: |
      <style>
        body {
          text-align: justify;
        }
        h1.title {
          text-align: center;
          padding-top: 120px;
        }

        /* Make plot outputs fill more width */
        .cell-output-display {
          max-width: 100% !important;
        }

        /* Optionally remove left/right padding */
        main.content {
          padding-left: 2rem;
          padding-right: 2rem;
        }
      </style>
format:
  html:
    theme: default
    page-layout: full
---


```{r, library}
#| echo: false
#| warning: false
#| message: false
library(tidyverse)
library(plotly)
```

```{r, source path}
#| include: false

# Define the base path for the project
# Adjust `base_path` to the appropriate root depending on the project (Journal Club or JoCO)
base_path <- here::here()  # For Journal Club, use project root

# For JoCO, it might be:
# base_path <- here::here("perspectives", "Vol_2_Issue_2", "nivo_rela_nivo_ipi_itc")

# Define directories relative to `base_path`
files_dir <- file.path(base_path, "files")
img_dir <- file.path(base_path, "img")
scripts_dir <- file.path(base_path, "scripts")

# Load necessary libraries
library(tidyverse)
library(plotly)
library(gt)
library(glue)

## Print paths to confirm
#message("Files directory: ", files_dir)
#message("Image directory: ", img_dir)
#message("Scripts directory: ", scripts_dir)

```



```{r load redcap data, warning=FALSE, message=FALSE, echo=FALSE}
#| eval: true
source(file.path(scripts_dir, "REDCap Pre JC Questions.R"))

```

---

```{r role, echo=FALSE}
#| eval: true
source(file.path(scripts_dir, "Question1_WhoAreYou.R"))

role_plot
```


<div style="text-align: right; margin-top: 2rem;">
  <a href="../experience/" class="btn btn-primary">Next →</a>
</div>
